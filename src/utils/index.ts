/**
 * Utils Module Index
 *
 * Exports utility functions for pattern matching and text processing.
 */

export {
  extractAction,
  extractResource,
  extractWorkspace,
  extractNamespace,
  extractResourceName,
  tokenize,
  similarity,
  findBestMatch,
  getActionSynonyms,
  getResourceSynonyms,
  normalizeAction,
  normalizeResource,
} from './pattern-matcher';
